<p style="color: green"><%= notice %></p>

<h1>Stores</h1>

<div class="gridable" id="stores">
  <% @stores.each do |store| %>
    <%= link_to(store) do %>
      <%= render store %>
    <% end %>
  <% end %>
</div>

<%= link_to "New store", new_store_path %>
